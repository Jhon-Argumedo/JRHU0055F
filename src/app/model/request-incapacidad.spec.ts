import { RequestIncapacidad } from './request-incapacidad';

describe('RequestIncapacidad', () => {
  it('should create an instance', () => {
    expect(new RequestIncapacidad()).toBeTruthy();
  });
});
