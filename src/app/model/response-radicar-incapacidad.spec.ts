import { ResponseRadicarIncapacidad } from './response-radicar-incapacidad';

describe('ResponseRadicarIncapacidad', () => {
  it('should create an instance', () => {
    expect(new ResponseRadicarIncapacidad()).toBeTruthy();
  });
});
