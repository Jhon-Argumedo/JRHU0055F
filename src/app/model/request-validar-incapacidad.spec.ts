import { RequestValidarIncapacidad } from './request-validar-incapacidad';

describe('RequestValidarIncapacidad', () => {
  it('should create an instance', () => {
    expect(new RequestValidarIncapacidad()).toBeTruthy();
  });
});
