import { ResponseObservaciones } from './response-observaciones';

describe('ResponseObservaciones', () => {
  it('should create an instance', () => {
    expect(new ResponseObservaciones()).toBeTruthy();
  });
});
