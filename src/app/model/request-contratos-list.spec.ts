import { RequestContratosList } from './request-contratos-list';

describe('RequestContratosList', () => {
  it('should create an instance', () => {
    expect(new RequestContratosList()).toBeTruthy();
  });
});
