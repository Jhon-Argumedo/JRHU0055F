import { DocumentoRadicacion } from './documento-radicacion';

describe('DocumentoRadicacion', () => {
  it('should create an instance', () => {
    expect(new DocumentoRadicacion()).toBeTruthy();
  });
});
