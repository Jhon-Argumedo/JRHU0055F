import { SubtipoIncapacidad } from './subtipo-incapacidad';

describe('SubtipoIncapacidad', () => {
  it('should create an instance', () => {
    expect(new SubtipoIncapacidad()).toBeTruthy();
  });
});
