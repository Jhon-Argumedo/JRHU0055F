import { RequestCargarDocumento } from './request-cargar-documento';

describe('RequestCargarDocumento', () => {
  it('should create an instance', () => {
    expect(new RequestCargarDocumento()).toBeTruthy();
  });
});
